---
description: Before finishing any task, verify the app works and there are no errors
alwaysApply: true
---

# EMDR — verify before done

Before ending your turn on any task that changes code, config, or behavior:

## Required checks

1. **Lint edited files** — run `ReadLints` on every file you created or modified; fix new issues you introduced.
2. **Run tests** — `npm test` in the repo root. If tests fail, fix or explain before finishing.
3. **ESLint / project lint** — always run `npm run lint` (`next lint`) when code/config changed. Fix all errors before ending or pushing. This is the local stand-in for “would CI/Docker build fail on lint?” — **do not** run `npm run build` just to verify (too slow; Actions builds the image).
4. **Runtime smoke** — when touching API routes, auth, middleware, or DB schema:
   - confirm the dev server starts or is already running on port **3471**;
   - hit the affected route(s) or check terminal output for **500**, **tuple concurrently updated**, or stack traces;
   - fix regressions before reporting done.
5. **Mobile shell** — when touching any page, layout, nav/tabs, or page-level CSS: verify at ~**390×844** (screenshot or measured layout) per `.cursor/rules/mobile-shell-check.mdc`. Do not ship UI checked only on desktop.
6. **Copy + design review** — when touching a page or user-facing UI: run **/redpen**, **/writing-copy**, and **/frontend-design** per `.cursor/rules/page-copy-design-review.mdc`. If issues remain, propose and apply fixes before ending.
7. **Report honestly** — if something could not be verified (no DB, no network, missing env, no browser), say what was checked, what was skipped, and why.

## Do not

- End a coding turn without running at least **`npm run lint` + `npm test`** when code changed.
- Push / ship without a green lint + test run in the same turn (see `git-commit-push`).
- Run a full `npm run build` as a pre-push gate unless the user explicitly asks — prefer lint + tests.
- Assume success without evidence from commands or terminal output.
- Ignore errors in the dev server log the user pasted or that appear in your shell.

## Quick command reference

```bash
npm test
npm run lint
```

Postgres (if schema/auth touched): `npm run db:up` — Docker on port **5434**.
